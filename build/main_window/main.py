
# This file was generated by the Tkinter Designer by Parth Jadhav
# https://github.com/ParthJadhav/Tkinter-Designer


from pathlib import Path

# from tkinter import *
# Explicit imports to satisfy Flake8
from tkinter import Tk, Canvas, Entry, Text, Button, PhotoImage, Toplevel, messagebox
from build.main_window.dashboard.gui5 import Dashboard
from build.main_window.records.main import Record
OUTPUT_PATH = Path(__file__).parent
ASSETS_PATH = OUTPUT_PATH / Path(r"./assets")


def relative_to_assets(path: str) -> Path:
    return ASSETS_PATH / Path(path)


def mainWindow():
    MainWindow()
    
    
    
class MainWindow(Toplevel):
    def __init__(self, *args, **kwargs):
        Toplevel.__init__(self, *args, **kwargs)
        self.title("Traffic Record System")
        self.geometry("1200x800")
        self.configure(bg = "#FBF6F6")


        canvas = Canvas(
            self,
            bg = "#FBF6F6",
            height = 800,
            width = 1200,
            bd = 0,
            highlightthickness = 0,
            relief = "ridge"
        )

        canvas.place(x = 0, y = 0)
        self.image_image_1 = PhotoImage(
            file=relative_to_assets("image_1.png"))
        image_1 = canvas.create_image(
            600.0,
            41.0,
            image=self.image_image_1
        )

        canvas.create_rectangle(
            0.0,
            156.0,
            1200.0,
            800.0,
            fill="#FFFFFF",
            outline="")

        self.button_image_1 = PhotoImage(
            file=relative_to_assets("button_1.png"))
        self.dash_btn = Button(
            self,
            image=self.button_image_1,
            borderwidth=0,
            highlightthickness=0,
            command=lambda: self.handle_btn_press(self.dash_btn,"dash"),
            relief="flat"
        )
        self.dash_btn.place(
            x=9.0,
            y=92.0,
            width=205.0,
            height=55.0
        )

        self.button_image_2 = PhotoImage(
            file=relative_to_assets("button_2.png"))
        self.record_btn = Button(
            self,
            image=self.button_image_2,
            borderwidth=0,
            highlightthickness=0,
            command=lambda: self.handle_btn_press(self.record_btn,"record"),
            relief="flat"
        )
        self.record_btn.place(
            x=223.0,
            y=92.0,
            width=205.0,
            height=55.0
        )

        self.button_image_3 = PhotoImage(
            file=relative_to_assets("button_3.png"))
        button_3 = Button(
            self,
            image=self.button_image_3,
            borderwidth=0,
            highlightthickness=0,
            command=self.logout,
            relief="flat"
        )
        button_3.place(
            x=985.0,
            y=92.0,
            width=205.0,
            height=55.0
        )
   
        self.windows = {
            "dash": Dashboard(self),
            "record": Record(self),
               
         }
     
        
        self.handle_btn_press(self.dash_btn, "dash")
        self.resizable(False, False)
        self.mainloop()
        self.current_window.place(x=75, y=0, width=1058, height=622)
        self.current_window.tkraise()
    
     
    def place_sidebar_indicator(self):
        pass    

    def logout(self):
        confirm = messagebox.askyesno(
        "Confirm log-out", "Do you really want to log out?"
      )
        if confirm == True:
            user = None
            self.destroy()
            #LoginPota()
    
    def handle_btn_press(self, caller, name):
    
       

        for window in self.windows.values():
            window.place_forget()


        self.current_window = self.windows.get(name)


        self.windows[name].place(x=0, y=156, width=1200, height=644)

