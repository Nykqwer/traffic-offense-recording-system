
# This file was generated by the Tkinter Designer by Parth Jadhav
# https://github.com/ParthJadhav/Tkinter-Designer


from pathlib import Path

# from tkinter import *
# Explicit imports to satisfy Flake8

from tkinter import Frame, Tk, Canvas, Entry, Text, Button, PhotoImage
from build.main_window.records.view_record.gui1 import viewRecord
from build.main_window.records.add_record.gui2 import addRecord
from build.main_window.records.update_record.gui3 import updateRecord
from build.main_window.records.view_info.gui4 import viewInfo
#import controller as db_controller

OUTPUT_PATH = Path(__file__).parent
ASSETS_PATH = OUTPUT_PATH / Path(r"./assets")


def relative_to_assets(path: str) -> Path:
    return ASSETS_PATH / Path(path)



def record():
    Record()
    

class Record(Frame):
     def __init__(self, parent, controller=None, *args, **kwargs):
        Frame.__init__(self, parent, *args, **kwargs)
        self.parent = parent
        self.selected_rid = None
        self.configure(bg="#FFFFFF")
        #self.product_data = db_controller.get_product()
          # Insert data
        
        self.windows = {
            "view": viewRecord(self),
            "add": addRecord(self),
            "up": updateRecord(self),
            "info": viewInfo(self),
       
        }
        
        self.current_window = self.windows["view"]
        self.current_window.place(x=0, y=0, width=1200, height=644)
    
        self.current_window.tkraise()

     def navigate(self, name):
        # Hide all screens
        for window in self.windows.values():
            window.place_forget()

        # Show the screen of the button pressed
        self.windows[name].place(x=0, y=0, width=1200, height=644)
